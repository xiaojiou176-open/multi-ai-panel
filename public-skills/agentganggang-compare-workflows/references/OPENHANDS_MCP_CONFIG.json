{
  "mcpServers": {
    "multi-ai-panel": {
      "command": "npm",
      "args": [
        "--prefix",
        "/absolute/path/to/MultiAiPanel",
        "run",
        "mcp:server"
      ]
    }
  }
}
